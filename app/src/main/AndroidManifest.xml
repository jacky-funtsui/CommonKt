<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ssf.commonkt">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".App">

        <!-- 声明UI设计稿的尺寸 -->
        <meta-data
            android:name="design_width"
            android:value="750" />
        <meta-data
            android:name="design_height"
            android:value="1334" />

        <activity android:name=".ui.welcome.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.main.MainActivity"/>
        <activity android:name=".ui.simple.SimpleListAdapterActivity"/>
        <activity android:name=".ui.simple.SimpleSupportAdapterActivity"/>
        <activity android:name=".ui.simple.SimpleHeaderFooterActivity"/>
        <activity android:name=".ui.simple.SimpleMultiAdapterActivity"/>
        <activity android:name=".ui.simple.SimpleDelegateAdapterActivity"/>
    </application>

</manifest>