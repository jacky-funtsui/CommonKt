<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white"
    android:orientation="vertical">
    <!-- 加载中布局 -->
    <include android:id="@+id/arr_rl_loading" android:visibility="gone" layout="@layout/layout_state_child_loading"/>
    <!-- 空布局 -->
    <include android:id="@+id/arr_rl_empty" android:visibility="gone" layout="@layout/layout_state_child_data"/>
    <!-- 网络重连 -->
    <include android:id="@+id/arr_rl_refresh" android:visibility="gone" layout="@layout/layout_state_child_net"/>
</FrameLayout>